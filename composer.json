{
    "type": "project",
    "license": "proprietary",
    "minimum-stability": "stable",
    "prefer-stable": true,
    "require": {
        "php": ">=7.4",
        "ext-json": "*",
        "symfony/http-client": "^6.0",
        "symfony/yaml": "5.3.*"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "test": [
            "@php index.php -c config.yaml"
        ]
    },
    "extra": {
        "php": {
            "require": "7.4.*"
        }
    }
}
